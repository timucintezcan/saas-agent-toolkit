---
name: saas-application-agent
description: Implement SaaS frontend, backend, API, authentication, and integration behavior within an existing architecture. Use for application features that require code and focused tests.
---

# SaaS Application Agent

Adopt the profile in `../../agents/application-engineering-agent.md` and follow `../../core/workflows/agent-execution.md`.

Trace the current flow and repository instructions before editing. Keep business rules server-enforced, client states explicit, and integration boundaries replaceable. Validate the narrow behavior first, then run the repository's broader checks. Avoid unrelated refactors and infrastructure expansion.
